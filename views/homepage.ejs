<%- include ('partial/navheader') %>
<!--------------------------Fiction Books---------------------------------->
<div id="fiction">
  <h2>FICTION</h2>
  <div class="row">
    <% data.forEach(function(book){ if(book.category === "fiction"){ %>
    <div class="col-lg-3 col-sm-6">
      <div class="card">
        <img class="card-img-top" src="<%= book.image %>" alt="" />
        <div class="card-body">
          <p class="card-title"><%= book.title %></p>
          <p class="card-text">
            by <%= book.author %>
          </p>
          <p class="card-text">
            <%= book.rating %> rating
            <span class="float-right">$<%= book.cost %></span>
          </p>
        </div>
      </div>
    </div>
    <%}});%>
  </div>
</div>
<!--------------------------Non Fiction Books---------------------------------->
<div id="nonfiction">
  <h2>NON FICTION</h2>
  <div class="row">
    <% data.forEach(function(book){ if(book.category === "nonfiction"){ %>
    <div class="col-lg-3 col-sm-6">
      <div class="card">
        <img class="card-img-top" src="<%= book.image %>" alt="" />
        <div class="card-body">
          <p class="card-title"><%= book.title %></p>
          <p class="card-text">
            by <%= book.author %>
          </p>
          <p class="card-text">
            <%= book.rating %> rating
            <span class="float-right">$<%= book.cost %></span>
          </p>
        </div>
      </div>
    </div>
    <%}});%>
  </div>
</div>

<%- include ('partial/navfooter') %>
